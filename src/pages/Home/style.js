import styled from "styled-components";

export const HomeWrapper = styled.div`
	margin-top: var(--nav-height);
	width: 100%;
	height: 100%;

	header {
		place-items: center;
		grid-row: 2;
		letter-spacing: 1px;
	}
`;
